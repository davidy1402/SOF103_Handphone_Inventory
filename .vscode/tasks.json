{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build & Run main (Mac)",
      "type": "shell",
      "command": "g++ main.cpp -o main && ./main",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "detail": "Compile and run main.cpp on Mac"
    },
    {
      "label": "Build & Run main (Windows)",
      "type": "shell",
      "command": "g++ main.cpp -o main.exe && main.exe",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build"
      },
      "problemMatcher": [],
      "detail": "Compile and run main.cpp on Windows"
    }
  ]
}
